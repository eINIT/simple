<module>
 <v-tftpd-gentoo
  name="TFTP Daemon (in.tftpd, Gentoo)"
  provides="tftpd"
  requires="network">

  <execute id="daemon" code=". /etc/conf.d/in.tftpd; in.tftpd -L ${INTFTPD_OPTS}" />

  <restart b="yes" />
  <need-files s="in.tftpd" />
 </v-tftpd-gentoo>
</module>
