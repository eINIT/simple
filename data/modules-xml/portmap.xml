<module>
 <v-portmap
  name="Port to RPC program number mapper (portmap)"
  provides="portmap"
  after="^net"
  before="xinetd" >

  <execute id="daemon" code="portmap -d" />

  <need-files s="portmap" />
  <restart b="yes" />
 </v-portmap>
</module>
