<module>
 <v-local
  name="local.start/local.stop"
  provides="local"
  after=".*">

  <execute id="enable" code="sh /etc/einit/conf.d/local.start; done" />
  <execute id="disable" code="sh /etc/einit/conf.d/local.stop; done" />
 </v-local>
</module>
