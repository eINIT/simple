<module>
 <v-local
  name="local.start/local.stop"
  provides="local"
  after=".*">

  <execute id="enable" code="sh /etc/einit/conf.d/local.start; true" />
  <execute id="disable" code="sh /etc/einit/conf.d/local.stop; true" />
 </v-local>
</module>
